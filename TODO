Slide: check if really one second
drawImage: ROUND NUMBERS!

require imports instead of es2015 modules - rewire-webpack dont support es2015 modules
tslint: "no-require-imports": false,





// const assert = chai.assert;
// let rewire = (<any>require)('rewire');

// import rewire = require('rewire');
//
// const Segments = rewire('../src/Segments');
//
// Segments.__set__('Segment', {a: 1});
//
// // import Segments from '../src/Segments';
// // const Segments = SegmentsImport.default;
// // Segments.__set__('S')
//
//
// describe('Segments', function() {
//     describe('prependSegment', function() {
//         it('', function() {
//             // let segments = new Segments({});
//             // segments.preloadSegments();
//             // segments.prependSegment();
//             // console.log(Segments);
//             // assert.ok(true);
//         });
//     });
// });
